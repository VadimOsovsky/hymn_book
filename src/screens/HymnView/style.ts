import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  lyricsView: {
    width: '100%',
    height: '100%',
    padding: 10,
  },
  lyricsText: {
    fontSize: 17,
    color: "#000",
  }
})
